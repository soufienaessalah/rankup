<form method="post" action="{{ path('app_match_entity_delete', {'id': match_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ match_entity.id) }}">
    <button class="btn">Delete</button>
</form>
