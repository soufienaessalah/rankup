<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Votre application Rank'Up</title>
    <style>
       body {
            background-color: #e6d0f2;
            font-family: Arial, sans-serif;
            color: #3f32a6;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            padding: 20px;
        }

        .content {
            flex-grow: 1;
        }

        .row {
            margin-bottom: 20px;
            background-color: #fff;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
        }

        footer {
            background-color: #2d143d;
            color: #fff;
            padding: 20px;
            text-align: center;
            margin-top: auto; /* Pour pousser le footer en bas de la page */
        }

        h1 {
            text-align: center;
            font-size: 2em;
            margin-bottom: 20px;
            color: #2d143d;
        }

        .logo {
            width: 100px; /* Set the width of the image */
            height: auto; /* Maintain aspect ratio */
            margin-bottom: 20px;
        }
    </style>
</head>

<body>
  <img class="ttr-logo-mobile" alt="" src="..public\images\logo.png" width="30" height="30">

    <h1>Bienvenue dans votre application Rank'Up</h1>

    <div class="content">
        <div class="row">
            <h2>nomLecon:{{ pdf.nomLecon}}</h2>
            <p><strong>URL:</strong> {{ pdf.url }}</p>
            <p><strong>prix:</strong> {{ pdf.prix }}</p>
            <p><strong>Description:</strong> {{ pdf.description }}</p>
        </div>

        <!-- Ajoutez plus de contenu ici -->
    </div>

    <footer>
        Rank'Up &copy; Your Website 2022
    </footer>

</body>
</html>